{
  "test": "rotate-secret.200",
  "description": "Test webhook secret rotation endpoint",
  "curl": "curl -XPOST http://localhost:3000/api/admin/webhook/rotate -H 'Authorization: Bearer <JWT>'",
  "expected": {
    "status": 200,
    "body": {
      "ok": true,
      "secret_last4": "string",
      "rotated_at": "string"
    }
  }
}
