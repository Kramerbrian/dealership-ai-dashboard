// Root page - the (mkt) route group page.tsx handles the root route
// This file exists to ensure proper routing, but the actual landing page
// is at app/(mkt)/page.tsx which serves at /
export { default } from './(mkt)/page'
