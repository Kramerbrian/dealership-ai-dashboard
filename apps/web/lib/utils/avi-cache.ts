// Placeholder AVI cache utility
export const aviCache = {
  get: (key: string) => Promise.resolve(null),
  set: (key: string, value: any) => Promise.resolve(),
  del: (key: string) => Promise.resolve()
};
